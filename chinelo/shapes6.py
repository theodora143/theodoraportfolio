from turtle import *
import math


# Cynthia
t = Turtle()

# Set Up your screen and starting position.
setup(1000,800)
x_pos = 0
y_pos = 0
t.setposition(x_pos, y_pos)

color = input("choose a color")

t.pencolor(color)
number=input("integer")

t.pendown()
sideString=input ('how many sides?')
s=integer(sideString)
for p in range of (sides)

#start a trapezoid
t.right(90)
t.forward(10)
t.right(120)
t.forward(20)













# Close window on click.
exitonclick()
